import styled from "styled-components";

export const StyledProductHero = styled.section`
  height: 33.9rem;
  background-color: var(--black);
  padding: 8.9rem 0 0 0;
  display: grid;
  place-items: center;

  h3 {
    color: var(--white);
    text-transform: uppercase;
  }
`;
